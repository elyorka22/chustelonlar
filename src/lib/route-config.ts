/**
 * Next.js segment config MUST be declared inline in each page/layout/route file.
 * Copy these three lines — they cannot be re-exported from a shared module.
 *
 * export const dynamic = "force-dynamic";
 * export const revalidate = 0;
 * export const fetchCache = "force-no-store";
 */
